version: "3.9"

services:
  db:
    image: postgres:15
    container_name: pyblazex_db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: pyblazex
    ports:
      - "5432:5432"

  redis:
    image: redis:7
    container_name: pyblazex_redis
    restart: always
    ports:
      - "6379:6379"

  api:
    build: .
    container_name: pyblazex_api
    restart: always
    depends_on:
      - db
      - redis
    ports:
      - "8000:8000"
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
